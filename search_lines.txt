\n--- Matches for pageWhDashboard ---\n3177: if (id === 'wh-dashboard') await pageWhDashboard();\n4995: async function pageWhDashboard() {\n\n--- Matches for wh-dashboard ---\n3115: warehouse: [{ id: 'wh-dashboard', ico: '📊', lbl: 'Dashboard' }, { id: 'wh-incoming', ico: '📦', lbl: 'Incoming' }, { id: 'wh-stock', ico: '📋', lbl: 'St\n3177: if (id === 'wh-dashboard') await pageWhDashboard();\n\n--- Matches for pageWhWaitlist ---\n3181: if (id === 'wh-waitlist') await pageWhWaitlist();\n5301: async function pageWhWaitlist() {\n5358: pollBadge(); pageWhWaitlist();\n5366: pollBadge(); pageWhWaitlist();\n\n--- Matches for wh-waitlist ---\n3115: warehouse: [{ id: 'wh-dashboard', ico: '📊', lbl: 'Dashboard' }, { id: 'wh-incoming', ico: '📦', lbl: 'Incoming' }, { id: 'wh-stock', ico: '📋', lbl: 'St\n3181: if (id === 'wh-waitlist') await pageWhWaitlist();\n5063: ${queue.length ? `<button class="btn btn-ghost btn-sm" onclick="nav('wh-waitlist')" style="width:100%;margin-top:10px">View Full Waitlist →</button>` \n\n--- Matches for pageSrWaitlist ---\n3182: if (id === 'sr-waitlist') await pageSrWaitlist();\n5370: async function pageSrWaitlist() {\n\n--- Matches for sr-waitlist ---\n3117: sales_rep: [{ id: 'sr-dashboard', ico: '📊', lbl: 'Dashboard' }, { id: 'sr-orders', ico: '📋', lbl: 'All Orders' }, { id: 'sr-waitlist', ico: '⏳', lbl: \n3182: if (id === 'sr-waitlist') await pageSrWaitlist();\n\n--- Matches for Waitlist ---\n2888: <!-- Global: Waitlist Options Modal (fallback when no #waitlist-options div on screen) -->\n2889: <div class="modal-bg" id="m-waitlist-modal" style="z-index:500">\n2893: <div id="m-waitlist-content"></div>\n2895: <button class="btn btn-ghost" onclick="closeModal('m-waitlist-modal')">Close</button>\n3115: warehouse: [{ id: 'wh-dashboard', ico: '📊', lbl: 'Dashboard' }, { id: 'wh-incoming', ico: '📦', lbl: 'Incoming' }, { id: 'wh-stock', ico: '📋', lbl: 'St\n3117: sales_rep: [{ id: 'sr-dashboard', ico: '📊', lbl: 'Dashboard' }, { id: 'sr-orders', ico: '📋', lbl: 'All Orders' }, { id: 'sr-waitlist', ico: '⏳', lbl: \n3181: if (id === 'wh-waitlist') await pageWhWaitlist();\n3182: if (id === 'sr-waitlist') await pageSrWaitlist();\n4104: <div id="waitlist-options" style="display:none;margin-top:16px"></div>\n4296: const toWaitlist = [], canPlace = [];\n4301: toWaitlist.push({ productId: p.productId, productName: p.productName, requestedQty: p.items, availableUnits: av,\n4308: if (toWaitlist.length) {\n4309: const issueList = toWaitlist.map(p =>\n4318: const mc = document.getElementById('m-waitlist-content');\n4326: <button class="btn btn-ghost" onclick="closeModal('m-waitlist-modal');if(window._pendingBatchBtn){window._pendingBatchBtn.disabled=false;window._pendi\n4327: <button class="btn btn-green" onclick="closeModal('m-waitlist-modal');_executeBatchWithWaitlist();">${canPlace.length ? 'Place available + Join Waitli\n4329: window._pendingBatch = { canPlace, toWaitlist, province, resolvedArea, btn };\n4331: openModal('m-waitlist-modal');\n4339: async function _executeBatchWithWaitlist() {\n4340: const { canPlace, toWaitlist, province, resolvedArea, btn } = window._pendingBatch || {};\n4342: for (const p of (toWaitlist || [])) {\n4343: await joinWaitlist(p.productId, p.productName, p.requestedQty, 'full', province, resolvedArea);\n4372: if (skipped) { showWaitlistOptions(r.failed, province, resolvedArea); } else { nav('my-orders'); }\n4375: showWaitlistOptions(r.failed, province, resolvedArea);\n4474: <div id="waitlist-options" style="display:none;margin-top:16px"></div>\n5000: const queue   = (d.waitlistQueue   || []);\n5012: <div class="sec-sub">Live stock overview · waitlist queue · low stock alerts</div></div>\n5022: ${kpiCard('Reserved (Partial Waitlist)', d.reservedUnits.toLocaleString(), `${d.partialWaitlist} partial entries`, '#c2410c')}\n5024: ${kpiCard('Pending Orders', d.pendingOrders, `Waitlist: ${d.fullWaitlist} full · ${d.partialWaitlist} partial`, 'var(--blue)')}\n5049: <!-- Waitlist Queue (FCFS) -->\n5051: <div class="card-title">⏳ Waitlist Queue\n5062: </div>`).join('') : `<div class="empty" style="padding:20px 0"><div class="empty-icon">⏳</div><div>No one on waitlist</div></div>`}\n5063: ${queue.length ? `<button class="btn btn-ghost btn-sm" onclick="nav('wh-waitlist')" style="width:100%;margin-top:10px">View Full Waitlist →</button>` \n5070: <span style="font-size:11px;font-weight:400;color:var(--brown-l);margin-left:6px">Available = Current − Reserved (partial waitlist)</span>\n5146: ${parseInt(p.waitingCount || 0) > 0 ? `<span class="badge b-navy" style="font-size:10px">⏳ ${p.waitingCount} waitlist</span>` : ''}\n5300: // ── WAREHOUSE WAITLIST ───────────────────────────\n5301: async function pageWhWaitlist() {\n5323: ${type === 'partial' ? `<button class="btn btn-green btn-sm" onclick="fulfilWaitlistEntry(${w.id})">✅ Fulfil</button>` : ''}\n5324: <button class="btn btn-ghost btn-sm" style="color:var(--red);border-color:var(--red)" onclick="removeWaitlistEntry(${w.id})">✕ Cancel</button>\n5333: <div class="sec-sub">Full waitlist = retailer waiting for full stock · Partial = retailer accepting available units</div></div>\n5337: 📋 Full Quantity Waitlist\n5345: ⚡ Partial Quantity Waitlist\n5353: async function fulfilWaitlistEntry(id) {\n5358: pollBadge(); pageWhWaitlist();\n5361: async function removeWaitlistEntry(id) {\n5366: pollBadge(); pageWhWaitlist();\n5369: // ── SALES REP WAITLIST (city-scoped) ─────────────\n5370: async function pageSrWaitlist() {\n5396: <div><div class="sec-title">Waitlist — Your City</div>\n5401: 📋 Full Quantity Waitlist\n5409: ⚡ Partial Quantity Waitlist\n6842: showWaitlistOptions(r.failed, cityEl.value, areaEl.value);\n6847: toast(`❌ All lines failed — see waitlist options below`, '#B71C1C');\n6848: showWaitlistOptions(r.failed, cityEl.value, areaEl.value);\n6852: function showWaitlistOptions(failed, city, area) {\n6862: onclick="joinWaitlist(${f.productId},'${escapeHtml(f.productName)}',${reqQty},'full','${escapeHtml(city)}','${escapeHtml(area)}')">\n6863: 📋 Waitlist for ${reqQty} units (full)\n6866: onclick="joinWaitlist(${f.productId},'${escapeHtml(f.productName)}',${avail},'partial','${escapeHtml(city)}','${escapeHtml(area)}')">\n6867: ⚡ Waitlist for ${avail} available (partial)\n6873: const formContainer = document.getElementById('waitlist-options');\n6878: const modalContent = document.getElementById('m-waitlist-content');\n6882: modalContent.innerHTML = html; openModal('m-waitlist-modal');\n6887: async function joinWaitlist(productId, productName, qty, type, city, area) {\n6894: toast(`✅ Added to ${label} waitlist — you will be notified when stock is ready 🔔`, '#0f766e');\n6897: document.querySelectorAll(`[onclick*="joinWaitlist(${productId},"]`).forEach(btn => {\n\n--- Matches for Waiting List ---\n3814: 📋 Join Waiting List\n4611: // Refresh stock info and open modal so retailer can choose partial / waiting list\n5112: <div class="sec-sub">Update stock — retailers on the waiting list and rejected orders are auto-notified 🔔</div></div>\n5170: 🔔 <strong>Auto-notify:</strong> Updating stock notifies waiting list retailers and those with rejected "Out of Stock" orders.\n5332: <div><div class="sec-title">Waiting List</div>\n5362: if (!confirm('Cancel this waiting list entry? The retailer will be notified and reserved stock will be returned.')) return;\n7013: ? `<strong>${prodName}</strong> is currently out of stock.<br><br>Join the waiting list and we will notify you as soon as stock is replenished.`\n7014: : `Only <strong>${avail} units</strong> of <strong>${prodName}</strong> are available.<br>You requested <strong>${requestedQty} units</strong>.<br><br\n7025: waitBtn.textContent = `📋 Join Waiting List for ${requestedQty} units`;\n7081: toast(`ℹ️ You are already on the waiting list for this product`); return;\n7083: toast(`📋 Added to waiting list — you'll be notified when stock is replenished 🔔`);\n\n--- Matches for Warehouse Dashboard ---\n4994: // ══ WAREHOUSE DASHBOARD ═════════════════════════\n5011: <div><div class="sec-title">Warehouse Dashboard</div>\n\n--- Matches for Dashboard ---\n3114: order_team: [{ id: 'ot-dashboard', ico: '📊', lbl: 'Dashboard' }, { id: 'deliveries', ico: '🚚', lbl: 'All Deliveries' }, { id: 'retailers', ico: '🏪', l\n3115: warehouse: [{ id: 'wh-dashboard', ico: '📊', lbl: 'Dashboard' }, { id: 'wh-incoming', ico: '📦', lbl: 'Incoming' }, { id: 'wh-stock', ico: '📋', lbl: 'St\n3117: sales_rep: [{ id: 'sr-dashboard', ico: '📊', lbl: 'Dashboard' }, { id: 'sr-orders', ico: '📋', lbl: 'All Orders' }, { id: 'sr-waitlist', ico: '⏳', lbl: \n3177: if (id === 'wh-dashboard') await pageWhDashboard();\n3188: if (id === 'sr-dashboard') await pageSrDashboard();\n3191: if (id === 'ot-dashboard') await pageOtDashboard();\n4804: // ══ NESTLÉ SALES MANAGER DASHBOARD ══════════════\n4805: async function pageOtDashboard() {\n4863: <div class="sec-title">Sales Analytics Dashboard</div>\n4994: // ══ WAREHOUSE DASHBOARD ═════════════════════════\n4995: async function pageWhDashboard() {\n4996: const d = await api('/warehouse/dashboard');\n5011: <div><div class="sec-title">Warehouse Dashboard</div>\n6399: async function pageSrDashboard() {\n6400: const d = await api('/sales-rep/dashboard');\n6404: <div class="sec-hdr"><div><div class="sec-title">Sales Rep Dashboard ${cityLabel}</div>\n